---
description: Legacy compatibility shim for the HyperFrames Zev video workflow.
---

# Legacy HyperFrames Zev v2

This slash command is deprecated. Use `$hyperframes-zev` for the canonical skill.

When invoked through `/hyperframes_zev_v2`, immediately load and follow `$hyperframes-zev`. Preserve the v2 behavior: draft the video script first, present it for approval, and do not scaffold, generate TTS, create captions, or render until the user explicitly approves.
